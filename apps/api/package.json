{
  "name": "@vedaai/api",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "uv run uvicorn grader.main:app --reload --port 8000",
    "test": "uv run --extra dev --extra aws --extra grading python -m pytest -q",
    "typecheck": "uv run --extra dev --extra aws --extra grading python -m ruff check src tests",
    "start": "uv run uvicorn grader.main:app --host 0.0.0.0 --port 8000"
  }
}
